# Create a Python script ('luckyNumber.py') that generates a random number between 1 and 10 (inclusive).
# Print the random number to the console.
import random

print('Random number generated from 1 - 10:', random.randint(1, 10))